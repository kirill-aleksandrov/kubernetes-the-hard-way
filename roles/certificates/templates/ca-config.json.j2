{
  "signing": {
    "default": {
      "expiry": "{{ certificate.ca.config.expiry }}"
    },
    "profiles": {
      "kubernetes": {
        "usages": ["signing", "key encipherment", "server auth", "client auth"],
        "expiry": "{{ certificate.ca.config.expiry }}"
      }
    }
  }
}
